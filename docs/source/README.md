# docs/source

Original assignment material, kept verbatim for reference — including its bugs (e.g. `app.js` has a missing comma before `guarantee` that makes it fail to parse). Do not fix it here; the corrected, typed version lives in `client/src/entities/*/model/seed.ts`.
