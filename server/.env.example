# Local dev defaults. In production, set CLIENT_URL in your hosting
# platform's environment variables to the deployed frontend's exact
# origin (see "Deployment" in the root README) - CORS requires an
# exact match.
PORT=4500
CLIENT_URL=http://localhost:5173
