# Local dev default. In production, set this in your hosting
# platform's environment variables to the deployed backend's URL
# (see "Deployment" in the root README) - it's baked in at build time.
VITE_SOCKET_URL=http://localhost:4500
